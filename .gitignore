# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables (never commit filled-in env)
.env
.env.local
.env.production
.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# VS Code
.vscode/

# Large web-ready audio: served from R2 in remote mode — never commit.
public/audio/
# NOTE: public/covers/ and public/reports/ ARE committed — they're small, cacheable,
# zero-egress, and the site needs them at build time (see src/scripts/ingest/uploadR2.mjs).
